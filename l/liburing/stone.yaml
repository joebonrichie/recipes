#
# SPDX-FileCopyrightText: © 2020-2024 Serpent OS Developers
#
# SPDX-License-Identifier: MPL-2.0
#
name        : liburing
version     : "2.13"
release     : 3
homepage    : https://github.com/axboe/liburing
upstreams   :
    - https://github.com/axboe/liburing/archive/refs/tags/liburing-2.13.tar.gz : 618e34dbea408fc9e33d7c4babd746036dbdedf7fce2496b1178ced0f9b5b357
summary     : Library providing helpers for the Linux kernel io_uring support
description : |
    This is the io_uring library, liburing. liburing provides helpers to setup and
    teardown io_uring instances, and also a simplified interface for
    applications that don't need (or want) to deal with the full kernel
    side implementation.
license     :
    - GPL-2.0-or-later
    - LGPL-2.1-or-later
    - MIT
setup       : |
    # non-compliant configure
    ./configure \
        --mandir=%(mandir) \
        --use-libc
build       : |
    %make
install     : |
    %make_install
packages    :
    - "%(name)-devel":
        paths:
            - /usr/share/man/

    - "%(name)-staticlib":
        rundeps:
            - "%(name)-devel"
        paths:
            - /usr/lib/lib*.a
