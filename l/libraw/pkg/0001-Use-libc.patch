From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Reilly Brogan <reilly@reillybrogan.com>
Date: Wed, 13 Aug 2025 14:19:11 -0500
Subject: [PATCH] Use libc++

---
 libraw.pc.in   | 2 +-
 libraw_r.pc.in | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/libraw.pc.in b/libraw.pc.in
index c95fbc48..9d883f56 100644
--- a/libraw.pc.in
+++ b/libraw.pc.in
@@ -7,6 +7,6 @@ Name: libraw
 Description: Raw image decoder library (non-thread-safe)
 Requires: @PACKAGE_REQUIRES@
 Version: @PACKAGE_VERSION@
-Libs: -L${libdir} -lraw -lstdc++@PC_OPENMP@
+Libs: -L${libdir} -lraw -lc++@PC_OPENMP@
 Libs.private: @PACKAGE_LIBS_PRIVATE@
 Cflags: -I${includedir}/libraw -I${includedir}
diff --git a/libraw_r.pc.in b/libraw_r.pc.in
index 92bbbffa..8997ef02 100644
--- a/libraw_r.pc.in
+++ b/libraw_r.pc.in
@@ -7,6 +7,6 @@ Name: libraw
 Description: Raw image decoder library (thread-safe)
 Requires: @PACKAGE_REQUIRES@
 Version: @PACKAGE_VERSION@
-Libs: -L${libdir} -lraw_r -lstdc++@PC_OPENMP@
+Libs: -L${libdir} -lraw_r -lc++@PC_OPENMP@
 Libs.private: @PACKAGE_LIBS_PRIVATE@
 Cflags: -I${includedir}/libraw -I${includedir}
