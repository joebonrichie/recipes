#
# SPDX-FileCopyrightText: © 2020-2025 Serpent OS Developers
#
# SPDX-License-Identifier: MPL-2.0
#
name        : ksanecore
version     : 25.08.3
release     : 4
upstreams   :
    - https://download.kde.org/stable/release-service/25.08.3/src/ksanecore-25.08.3.tar.xz : e15659994193ee60d29fae09e27d2e453fc32e449faf8808a57cac8a9eaba997
homepage    : https://invent.kde.org/libraries/ksanecore
license     :
    - LGPL-2.1-only
    - LGPL-3.0-only
summary     : Library providing logic to interface scanners
description : |
    KSaneCore is a library that provides a Qt interface for the SANE library for scanner hardware.
builddeps   :
    # Temporary, boulder doesn't handle build ordering resolution correctly when builddeps are added by macros, so add ECM manually
    - cmake(ECM)
    - cmake(KF6I18n)
    - pkgconfig(sane-backends)
setup       : |
    %cmake_kf6
build       : |
    %cmake_build
install     : |
    %cmake_install
# packages    :
#     - "%(name)-docs":
#         paths:
#             - /usr/share/doc
