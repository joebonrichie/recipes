#
# SPDX-FileCopyrightText: © 2020-2025 Serpent OS Developers
#
# SPDX-License-Identifier: MPL-2.0
#
name        : kaccounts-providers
version     : 25.12.0
release     : 11
upstreams   :
    - https://download.kde.org/stable/release-service/25.12.0/src/kaccounts-providers-25.12.0.tar.xz : a91fb669047c645611d12b77125d60bc6b13f65043bd437bbe68b90357fdb28c
homepage    : https://kde.org/
license     : GPL-2.0-or-later
summary     : KDE Providers
description : |
    KDE providers for accounts sign-on Provides additional configuration files for libaccounts and libsignon to recognize online services such as Facebook and Google.
builddeps   :
    # Temporary, boulder doesn't handle build ordering resolution correctly when builddeps are added by macros, so add ECM manually
    - cmake(ECM)
    - cmake(KF6CoreAddons)
    - cmake(KF6I18n)
    - cmake(KF6KIO)
    - cmake(KF6Package)
    - cmake(KAccounts6)
    - cmake(QCoro6)
    - cmake(Qt6Qml)
    - cmake(Qt6WebEngineQuick)
rundeps     :
    - binary(signon-ui)
    - signon-plugin-oauth2 # Google provider
setup       : |
    %cmake_kf6
build       : |
    %cmake_build
install     : |
    %cmake_install

    # Presumably unused config files (no reference in any related source project)
    rm -rfv %(installroot)/etc/signon-ui
    rmdir -v %(installroot)/etc
