#
# SPDX-FileCopyrightText: © 2020-2025 Serpent OS Developers
#
# SPDX-License-Identifier: MPL-2.0
#
name        : cpp-utilities
version     : 5.30.0
release     : 2
upstreams   :
    - https://github.com/Martchus/cpp-utilities/archive/refs/tags/v5.30.0.tar.gz : 9d594108109de7cd2dbb399451e69f68851a7f983e2671a0a4766211c380d1b4
homepage    : https://github.com/Martchus/cpp-utilities 
license     : GPL-2.0-or-later 
summary     : Common C++ classes and routines such as argument parser, IO and conversion utilities
description : |
    Common C++ classes and routines such as argument parser, IO and conversion utilities
builddeps   :
    - binary(doxygen)
    - cmake(Boost)
    # - pkgconfig(libarchive)
    - pkgconfig(openssl)
checkdeps   :
    - pkgconfig(cppunit)
setup       : |
    # Libarchive tests seem to fail, so don't build it for now
    %cmake \
        -DBUILD_SHARED_LIBS:BOOL=ON
build       : |
    %cmake_build
install     : |
    %cmake_install
check       : |
    # Odd, but whatever I guess
    # https://github.com/Martchus/cpp-utilities/issues/26
    %cmake_build --target check
packages    :
    - "%(name)-devel":
        paths:
            - /usr/share/c++utilities/
