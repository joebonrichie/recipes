releases:
  # Update this when switching to a new LTS series
  id: 374094
  rss: https://gitlab.isc.org/isc-projects/bind9/-/tags?format=atom
security:
  cpe:
  - vendor: isc
    product: bind
# Most CVEs will probably affect the bind-server only, so
# some of them might be ignored by us.
