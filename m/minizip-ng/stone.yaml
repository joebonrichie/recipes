#
# SPDX-FileCopyrightText: © 2020-2025 Serpent OS Developers
#
# SPDX-License-Identifier: MPL-2.0
#
name        : minizip-ng
version     : 4.0.10
release     : 4
summary     : Fork of the zlib zip library
license     : Zlib
homepage    : https://github.com/zlib-ng/minizip-ng
description : |
    Fork of the popular zip manipulation library found in the zlib distribution
upstreams   :
    - https://github.com/zlib-ng/minizip-ng/archive/refs/tags/4.0.10.tar.gz : c362e35ee973fa7be58cc5e38a4a6c23cc8f7e652555daf4f115a9eb2d3a6be7
builddeps   :
    - pkgconfig(bzip2)
    - pkgconfig(libcrypto)
    - pkgconfig(liblzma)
    - pkgconfig(libzstd)
    - pkgconfig(zlib-ng)
tuning      :
    - lto : full
setup       : |
    %cmake -B compat \
        -DBUILD_SHARED_LIBS=ON \
        -DMZ_COMPAT=ON \
        -DMZ_LIBBSD=OFF

    %cmake -B ng \
        -DBUILD_SHARED_LIBS=ON \
        -DMZ_COMPAT=OFF \
        -DMZ_LIBBSD=OFF
build       : |
    for b in compat ng; do
        %ccache_zero
        BUILDDIR=${b}
        env CCACHE_BASEDIR="${PWD}/${b}" %cmake_build
        %ccache_stats
    done
install     : |
    for b in compat ng; do
        BUILDDIR=${b}
        %cmake_install
    done
packages    :
    - "minizip":
        paths:
            - /usr/lib/libminizip.so.*
    - "minizip-devel":
        paths:
            - /usr/include/minizip
            - /usr/lib/cmake/minizip
            - /usr/lib/libminizip.so
            - /usr/lib/pkgconfig/minizip.pc
        rundeps:
            - minizip
