releases:
  id: 5440
  ignore:
    # We only update to the n-1 stable release. So for now we're only interested in 253.x updates
  - 258.*
  rss: ~
security:
  cpe:
  - vendor: systemd_project
    product: systemd
