#
# SPDX-FileCopyrightText: © 2020-2024 Serpent OS Developers
#
# SPDX-License-Identifier: MPL-2.0
#
name        : git
version     : 2.51.1
release     : 18
summary     : Fast, scalable, distributed revision control system
license     : GPL-2.0-only
homepage    : https://git-scm.com
description : |
    Git is a fast, scalable, distributed revision control system with an unusually rich command set that provides both high-level operations and full access to internals.
upstreams   :
    - https://www.kernel.org/pub/software/scm/git/git-2.51.1.tar.gz: b049d79e6a6cb3d81334bf689af6301f4d4c884191dfae65d2bb314a90384831
builddeps   :
    - binary(asciidoc)
    - binary(msgfmt)
    - binary(tar)
    - binary(xmlto)
    - pkgconfig(expat)
    - pkgconfig(gobject-2.0)
    - pkgconfig(libcurl)
    - pkgconfig(libpcre2-8)
    - pkgconfig(libsecret-1)
    - pkgconfig(libxcrypt)
    - pkgconfig(openssl)
    - pkgconfig(zlib)
    - docbook
environment : |
    %export_docbook_catalogs
setup       : |
    %reconfigure \
        --disable-static \
        --with-editor=/usr/bin/nano \
        --with-libpcre2 \
        --without-tcltk \
build       : |
    %make
    %make man
    %make -C contrib/subtree
    %make -C contrib/credential/libsecret
install     : |
    %make_install install-man
    %make_install -C contrib/subtree

    %install_file contrib/completion/git-completion.bash %(installroot)%(datadir)/bash-completion/completions/git
    %install_file contrib/completion/git-completion.zsh %(installroot)%(datadir)/zsh/site-functions/_git
    %install_exe contrib/credential/libsecret/git-credential-libsecret %(installroot)%(libdir)/git/git-core/.
# TODO Checks
packages    :
    - "%(name)":
        paths:
            - /usr/share/man/man3/
